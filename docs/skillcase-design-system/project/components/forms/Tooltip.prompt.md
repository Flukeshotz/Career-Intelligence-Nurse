# Tooltip

Dark tooltip with seven arrow positions.

```jsx
<Tooltip arrow="none" text1="Label" />
```

Variants: `arrow` (none, bottom left, bottom right, left, right, bottom center, top center)

Text slots (`text1`…) override the design's baked copy; see the `.d.ts` for defaults.
