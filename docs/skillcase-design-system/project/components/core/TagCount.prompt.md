# TagCount

Numeric counter appended to a tag.

```jsx
<TagCount size="sm" text1="Label" />
```

Variants: `size` (sm, md, lg)

Text slots (`text1`…) override the design's baked copy; see the `.d.ts` for defaults.
