# AvatarCompanyIcon

AvatarCompanyIcon — extracted verbatim from the Skillcase Figma file; use it wherever the source design uses it.

```jsx
<AvatarCompanyIcon size="xs" />
```

Variants: `size` (xs, sm, md, lg, xl, 2xl)
